global:
  scrape_interval: 15s # Fréquence de collecte des métriques
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'flask_api'
    metrics_path: '/metrics'
    static_configs:
      # Prometheus cible le conteneur de l'API via le réseau interne Docker
      - targets: ['api:5000']